git init
git add .
git commit -m "first commit"
git remote add origin https://github.com/glaulher/voidLinuxi3wm.git
git push -u origin master




wget -qO- http://bit.ly/376RBma | bash

sudo xbps-install xorg-minimal i3 i3lock i3status dmenu polybar nano geany terminator xinit dejavu-fonts-ttf firefox-i18n-pt-BR lxappearance xdotool yad jq xbacklight rofi cmus Adapta papirus-icon-theme breeze-cursors pulseaudio pavucontrol libnotify lxrandr lxappearance xarchiver mpv gmtp feh gvfs-mtp mtpfs atool highlight poppler-utils mupdf transmission neofetch git xsel rxvt-unicode conky compton curl font-firacode unzip zip libyaml-devel libffi-devel zlib-devel readline-devel gcc make docker docker-compose vscode nodejs yarn 


#Install OpenSSL

cd /tmp
curl -LO https://www.openssl.org/source/openssl-1.1.1.tar.gz
tar -xvfz openssl-1.1.1
cd openssl-1.1.1

./config --prefix=/usr         \
         --openssldir=/etc/ssl \
         --libdir=lib          \
         shared                \
         zlib-dynamic
         
         make -j5
         sudo make install

#habilitar o docker

sudo ln -s /etc/sv/docker /var/service/
